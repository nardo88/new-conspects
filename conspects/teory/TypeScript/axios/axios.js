const axios = {
    

    create(obj) {
        return {
            get(str) {
                return new Promise
            },
            post(str) {
                return new Promise
            },
        }
    }
}

export default axios